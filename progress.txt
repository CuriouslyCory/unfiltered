## Codebase Patterns
- ESLint 9 with flat config: use `eslint.config.mjs` (not `.eslintrc.cjs`), `eslint-config-next@16` exports flat config arrays
- Use `tseslint.configs.disableTypeChecked` for `.js`/`.mjs`/`.cjs` files to avoid project service errors
- `eslint-plugin-mdx` requires `import * as mdx` (no default export)
- Prisma typed SQL requires `prisma generate --sql` to regenerate after dependency updates
- Project uses T3 stack: Next.js + tRPC + Prisma + NextAuth
- Lint scripts should exclude non-source dirs (tasks, .agents, prisma, scripts) and *.md files
- Zod v4: use `z.ZodError` (not `ZodError`) and `z.flattenError(err)` (not `err.flatten()`)
- Prisma v7: generator provider is `prisma-client` (not `prisma-client-js`), output goes to `src/generated/prisma/`
- Prisma v7: use `PrismaPg` adapter from `@prisma/adapter-pg` with `{ connectionString }` (not raw string)
- Prisma v7: config uses `migrations.path` (not `migrate.migrations`), no `earlyAccess` property
- Prisma v7: imports from `~/generated/prisma/client` (not `@prisma/client`), SQL from `~/generated/prisma/sql`
- Prisma v7: use extensionless imports (`~/generated/prisma/client` not `client.js`) — Turbopack can't resolve `.js` extension to `.ts` files
- react-day-picker v9: classNames use new keys (e.g., `month_caption`, `day_button`, `selected`, `today`, `outside`), `Chevron` component replaces `IconLeft`/`IconRight`

---

## 2026-03-12 - US-001
- What was implemented: Patch and minor dependency bumps for all safe dependencies
- Files changed:
  - package.json (updated deps, changed lint scripts from `next lint` to `eslint .`)
  - pnpm-lock.yaml (lockfile updated)
  - .eslintignore (new file to exclude non-source files from eslint)
- **Learnings for future iterations:**
  - Next.js 16 removed `next lint` — the `lint` and `check` scripts needed updating to use `eslint .` directly
  - @types/react 18→19 upgrade caused no type errors despite React 19 runtime already being installed
  - @t3-oss/env-nextjs went from 0.10→0.13 (minor-ish) with no breaking changes
  - TypeScript 5.9.3 causes peer dep warnings with @typescript-eslint/* 8.28.0 (needs <5.9.0) — will be resolved in US-003
  - `prisma generate --sql` must be re-run after dependency updates to regenerate typed SQL exports
---

## 2026-03-12 - US-002
- What was implemented: Zod v3→v4 migration and @hookform/resolvers v3→v5
- Files changed:
  - package.json (zod ^4.3.6, @hookform/resolvers ^5.2.2)
  - pnpm-lock.yaml (lockfile updated)
  - src/server/api/trpc.ts (replaced `ZodError` import with `z`, updated errorFormatter to use `z.ZodError` and `z.flattenError()`)
- **Learnings for future iterations:**
  - Zod v4 migration is minimal for this codebase — all schema APIs (z.object, z.string, z.number, z.boolean, z.date, z.enum, .optional, .default, .url, .nullable) are stable
  - Main breaking change is `ZodError` → `z.ZodError` and `.flatten()` → `z.flattenError()`
  - @t3-oss/env-nextjs, @trpc/server, and @hookform/resolvers all compatible with Zod v4 out of the box
  - @hookform/resolvers is installed but not yet imported in any source files
---

## 2026-03-12 - US-003
- What was implemented: Next.js 16.0→16.1, tRPC 11.0→11.12, next-auth beta.25→beta.30, @auth/prisma-adapter 2.8→2.11, ESLint 8→9, eslint-config-next 15→16, @typescript-eslint/* 8.28→8.57, eslint-plugin-mdx 3.3→3.7
- Files changed:
  - package.json (upgraded deps, removed @types/eslint)
  - pnpm-lock.yaml (lockfile updated)
  - .eslintrc.cjs (deleted — replaced by flat config)
  - .eslintignore (deleted — ignores now in eslint.config.mjs)
  - eslint.config.mjs (new — ESLint 9 flat config)
  - src/server/api/trpc.ts (optional chaining fix for `@typescript-eslint/prefer-optional-chain`)
- **Learnings for future iterations:**
  - `eslint-config-next@16` only supports flat config — legacy `.eslintrc.cjs` causes circular structure errors
  - `eslint-plugin-mdx` has no default export — must use `import * as mdx from "eslint-plugin-mdx"` and `mdx.flat` for the flat config object
  - JS/MJS config files must have type-checked rules disabled via `tseslint.configs.disableTypeChecked` or they'll fail with "not found by the project service"
  - @typescript-eslint/* 8.57 resolves the TypeScript 5.9.3 peer dep warnings from 8.28
  - next-auth beta.25→beta.30 and tRPC 11.0→11.12 were drop-in upgrades with no breaking changes
  - `@typescript-eslint/prefer-optional-chain` is now enforced as error in `recommendedTypeChecked`
---

## 2026-03-12 - US-004
- What was implemented: Prisma v7 schema, config, and adapter-based client setup
- Files changed:
  - package.json (@prisma/client 6.5→7.5, prisma 6.5→7.5, added @prisma/adapter-pg, tsx, dotenv)
  - pnpm-lock.yaml (lockfile updated)
  - prisma/schema.prisma (generator: prisma-client with output, removed fullTextSearchPostgres preview feature, removed datasource url)
  - prisma.config.ts (new — defineConfig with schema, migrations path, and datasource url)
  - src/server/db.ts (rewritten to use PrismaPg adapter with generated client import)
  - .gitignore (added src/generated/)
- **Learnings for future iterations:**
  - Prisma v7 generator provider changed from `prisma-client-js` to `prisma-client`
  - `fullTextSearchPostgres` is GA in v7, must be removed from previewFeatures
  - `PrismaPg` constructor takes `pg.Pool | pg.PoolConfig`, not a raw connection string — use `{ connectionString: url }`
  - `prisma.config.ts` uses `migrations: { path }` not `migrate: { migrations }`, no `earlyAccess` property
  - Generated client output goes to `src/generated/prisma/` — typed SQL exports are at `~/generated/prisma/sql`
  - `--sql` flag still exists in Prisma v7 `prisma generate`
  - Typecheck/lint errors in other files are expected until US-005 updates all `@prisma/client` imports to `~/generated/prisma/client.js`
  - `datasource` block in schema.prisma no longer needs `url` — it's provided via prisma.config.ts
---

## 2026-03-12 - US-005
- What was implemented: Updated all `@prisma/client` imports to `~/generated/prisma/client` and `@prisma/client/sql` to `~/generated/prisma/sql`
- Files changed:
  - src/lib/document-utils.ts
  - src/app/_components/document-card.tsx
  - src/app/_components/document-type-badge.tsx
  - src/app/_components/document-table/columns.tsx
  - src/app/_components/document-table/data-table.tsx
  - src/app/admin/documents/columns.tsx
  - src/app/admin/documents/admin-documents-table.tsx
  - src/app/admin/_components/document-editor.tsx
  - src/app/eo-summary/[slug]/_components/artifact-section.tsx
  - src/app/eo-summary/[slug]/_components/summary-section.tsx
  - src/app/eo-summary/[slug]/_components/details-pane.tsx
  - src/app/eo-summary/[slug]/_components/updates-section.tsx
  - src/server/api/routers/document.ts (typed SQL import)
  - src/server/db.ts (PrismaClient import)
- **Learnings for future iterations:**
  - Turbopack (Next.js build) cannot resolve `.js` extensions to `.ts` files — use extensionless imports for generated Prisma client (`~/generated/prisma/client` not `client.js`)
  - TypeScript `tsc --noEmit` accepts both `.js` and extensionless imports with `moduleResolution: "Bundler"`, but Turbopack only accepts extensionless
  - `PrismaAdapter(db)` in auth config required no changes — it imports `db` from `~/server/db` which was already updated in US-004
  - All Prisma types (Document, DocumentArtifact, DocumentType) are re-exported from the generated `client.ts`
  - Typed SQL exports (e.g., `getDocumentFts`) are available from `~/generated/prisma/sql`
---

## 2026-03-12 - US-006
- What was implemented: Major version upgrades for tailwind-merge (2→3), sonner (1→2), and @vercel/analytics (1→2)
- Files changed:
  - package.json (updated deps)
  - pnpm-lock.yaml (lockfile updated)
- **Learnings for future iterations:**
  - tailwind-merge v3 is a drop-in upgrade — `twMerge()` and `cn()` APIs unchanged, internals updated for Tailwind v4 class resolution
  - sonner v2 (not v3 as PRD notes suggested) is the latest — `Toaster` component and `toast()` function APIs unchanged
  - @vercel/analytics v2 is a drop-in upgrade — `Analytics` component import from `@vercel/analytics/react` unchanged
  - All three upgrades required zero code changes, only package version bumps
---

## 2026-03-12 - US-007
- What was implemented: react-day-picker 8.10.1→9.14.0 and react-markdown 9.1.0→10.1.0
- Files changed:
  - package.json (updated react-day-picker and react-markdown versions)
  - pnpm-lock.yaml (lockfile updated)
  - src/app/_components/ui/calendar.tsx (rewritten for react-day-picker v9 API)
- **Learnings for future iterations:**
  - react-day-picker v9 completely renamed classNames keys: `caption` → `month_caption`, `nav_button` → split into `button_previous`/`button_next`, `table` → `month_grid`, `head_row` → `weekdays`, `head_cell` → `weekday`, `row` → `week`, `cell` → `day`, `day` → `day_button`, `day_selected` → `selected`, `day_today` → `today`, `day_outside` → `outside`, `day_disabled` → `disabled`, `day_range_start` → `range_start`, `day_range_end` → `range_end`, `day_range_middle` → `range_middle`, `day_hidden` → `hidden`
  - react-day-picker v9 replaced `IconLeft`/`IconRight` components with single `Chevron` component that receives `orientation` prop ("left" or "right")
  - react-markdown v10 is a drop-in upgrade — `Markdown` default export, `Components` type export, and `remarkPlugins` prop all unchanged
  - react-markdown v10 adds `MarkdownAsync` and `MarkdownHooks` named exports for async/hooks patterns
---

## 2026-03-13 - US-001
- What was implemented: `calculateDocumentHealth()` and `artifactSectionId()` utility functions in `src/lib/document-utils.ts`
- Files changed:
  - src/lib/document-utils.ts (added HealthScoreResult type, CategoryBreakdown type, calculateDocumentHealth function, artifactSectionId function)
- **Learnings for future iterations:**
  - Document model required fields: title, originalDocumentUrl, dateSigned, signer, type — all are non-nullable in Prisma schema
  - Optional fields: shortSummary (String?), riskScore (Int?) — use `!= null` check for riskScore since 0 is a valid value
  - `artifactOrder` already existed in document-utils.ts with the 9 standard artifact types
  - `getArtifactStyle()` also already existed for border colors and icons per artifact type
---

## 2026-03-13 - US-002
- What was implemented: Added section IDs and canonical artifact sort order to document editor
- Files changed:
  - src/app/admin/_components/document-editor.tsx (added imports for artifactOrder/artifactSectionId, added id="document-details" to details section, added id and scroll-mt-4 to artifact divs, replaced localeCompare sort with artifactOrder-based sort)
- **Learnings for future iterations:**
  - Artifact divs in document-editor.tsx now have stable IDs via `artifactSectionId()` — sidebar components can target these for scroll navigation
  - Artifacts not in `artifactOrder` sort to the end (using `artifactOrder.length` as sentinel value)
  - The document-editor has two main sections: "Document Details" (form) and "Document Artifacts" (list) — section IDs target the outer `<section>` elements
---

## 2026-03-13 - US-003
- What was implemented: Applied artifact-specific border colors and icons to artifact cards in the document editor
- Files changed:
  - src/app/admin/_components/document-editor.tsx (imported getArtifactStyle, replaced uniform border-gray-700 with border-l-4 + dynamic borderClass, added ArtifactIcon next to title)
- **Learnings for future iterations:**
  - `getArtifactStyle()` returns `{ borderClass, icon }` — destructure `icon` as a capitalized component name (e.g., `const ArtifactIcon = style.icon`) for JSX usage
  - Changed `.map()` with implicit return to block body with explicit return to accommodate the `const style = ...` binding
  - Browser verification requires auth (Discord login) — manual verification needed for UI stories unless session cookies are pre-configured
---

## 2026-03-13 - US-004
- What was implemented: Added Published/Draft badge next to document title in admin document detail page header
- Files changed:
  - src/app/admin/documents/[id]/page.tsx (imported Badge component, added conditional badge with default variant for Published, secondary variant for Draft)
- **Learnings for future iterations:**
  - Badge component is at `~/app/_components/ui/badge` (not `~/components/ui/badge`)
  - `document.published` is a boolean field from Prisma schema, defaults to false
  - Pre-existing lint error on line 85 (`searchParam || undefined` should use `??`) — not introduced by this story
---

## 2026-03-13 - US-005
- What was implemented: Added NEXT_PUBLIC_WORKFLOW_API_URL client environment variable to src/env.js
- Files changed:
  - src/env.js (added NEXT_PUBLIC_WORKFLOW_API_URL to client schema with z.string().url().default('http://localhost:3200'), added to runtimeEnv mapping)
- **Learnings for future iterations:**
  - @t3-oss/env-nextjs client vars require both `client` schema entry AND `runtimeEnv` mapping with `process.env.NEXT_PUBLIC_*`
  - Zod `.default()` in client schema means the env var is optional at runtime — no `.env` entry needed for local dev
  - env.js is a `.js` file (not `.ts`) — it uses `tseslint.configs.disableTypeChecked` in the ESLint flat config
---

## 2026-03-13 - US-006
- What was implemented: Created DocumentSidebar client component with collapsible health score card
- Files changed:
  - src/app/admin/_components/document-sidebar.tsx (new file — DocumentSidebar component with SVG progress ring, collapsible breakdown)
- **Learnings for future iterations:**
  - Card component is at `~/app/_components/ui/card` (not `~/components/ui/card`)
  - Collapsible component is at `~/app/_components/ui/collapsible` — wraps @radix-ui/react-collapsible
  - SVG circular progress ring: use `stroke-dasharray={circumference}` and `stroke-dashoffset={circumference - (pct/100) * circumference}` with `rotate(-90)` to start from top
  - Health color thresholds: green (80+), yellow (60-79), orange (40-59), red (0-39)
  - Component is not yet integrated into the page layout — US-009 will add it to the document detail page
---

## 2026-03-13 - US-007
- What was implemented: Added artifact navigation list to DocumentSidebar with Document Details entry and all 9 standard artifacts
- Files changed:
  - src/app/admin/_components/document-sidebar.tsx (added Navigation card with scrollToSection, artifact icons/colors, present/missing indicators)
- **Learnings for future iterations:**
  - Renamed `document` prop destructuring to `doc` to avoid shadowing the global `document` object needed by `document.getElementById()`
  - Used `style.borderClass.replace("border-", "text-")` to convert border color classes to text color classes for artifact icons
  - Present artifacts get a filled green dot (`bg-green-500`); missing artifacts get a hollow dot (`border border-muted-foreground`) with reduced opacity
  - Missing artifacts have `cursor-default opacity-50` and clicking them does nothing (no scroll target exists)
---

## 2026-03-13 - US-008
- What was implemented: Added IntersectionObserver-based active section highlighting to DocumentSidebar navigation
- Files changed:
  - src/app/admin/_components/document-sidebar.tsx (added useEffect/useRef imports, IntersectionObserver with rootMargin '-20% 0px -70% 0px', activeSection state, border-l-2 + bg-muted/50 highlight with 150ms transition)
- **Learnings for future iterations:**
  - IntersectionObserver rootMargin '-20% 0px -70% 0px' creates a narrow detection band in the top 20-30% of viewport — good for "which section am I reading" detection
  - Active highlight uses `border-l-2 border-primary bg-muted/50 pl-1.5` — the pl-1.5 compensates for the border width to prevent layout shift
  - `transition-all duration-150` on all nav buttons ensures smooth highlight changes regardless of which item becomes active
  - Observer depends on `doc.documentArtifact` to re-observe when artifacts change (e.g., after generation)
  - eslint-disable for react-hooks/exhaustive-deps needed because `presentTitles` is a Set created each render — the dep on `doc.documentArtifact` is sufficient
---

## 2026-03-13 - US-009
- What was implemented: Restructured admin document detail page to sidebar + main content flex layout
- Files changed:
  - src/app/admin/documents/[id]/page.tsx (imported DocumentSidebar, wrapped content in flex layout with sticky sidebar)
- **Learnings for future iterations:**
  - DocumentSidebar is a client component imported into a server component page — no issues since it receives serializable props
  - Sidebar uses `hidden lg:block` for responsive behavior — single-column layout preserved below lg breakpoint
  - Sticky sidebar wrapper uses `sticky top-4` with `max-h-[calc(100vh-2rem)] overflow-y-auto` for scroll containment
  - The `self-start` class is on the inner div (not aside) to work with the sticky positioning
  - Pre-existing lint error on line 86 (`searchParam || undefined` should use `??`) — not from this story
---

## 2026-03-13 - US-010
- What was implemented: Added artifact generation buttons to DocumentSidebar for missing artifacts, with route mapping to workflow API
- Files changed:
  - src/app/admin/_components/document-sidebar.tsx (added artifactRouteMap, generateArtifact/generateAllMissing functions, Play/Loader2/Sparkles icons, TooltipProvider for deprecated "Areas of Concern", "Generate All Missing" button)
- **Learnings for future iterations:**
  - `artifactRouteMap` maps artifact titles to API routes — "Areas of Concern" has no route and is marked deprecated with a tooltip
  - Generate buttons use `env.NEXT_PUBLIC_WORKFLOW_API_URL` from `~/env` (client-side env var)
  - Loading state tracked via `Set<string>` — individual artifacts use their title as key, "Generate All Missing" uses `"__all__"` sentinel
  - `router.refresh()` from `next/navigation` refreshes server components to pick up newly generated artifacts
  - Tooltip component is at `~/app/_components/ui/tooltip` — wraps @radix-ui/react-tooltip, needs TooltipProvider wrapper
  - sonner's `toast.success()` and `toast.error()` used for generation feedback
---

## 2026-03-13 - US-011
- What was implemented: Added Ctrl+E / Cmd+E keyboard shortcut to toggle document details edit mode
- Files changed:
  - src/app/admin/_components/document-editor.tsx (added useEffect import, keydown listener with activeElement check)
- **Learnings for future iterations:**
  - `document` prop shadows `globalThis.document` in DocumentEditor — use `globalThis.document.activeElement` to access the DOM
  - Keyboard shortcut skips activation when focused on INPUT, TEXTAREA, or SELECT elements, or when editing an artifact (`editingArtifactId !== null`)
  - `setIsEditing((prev) => !prev)` used as functional updater to avoid stale closure over `isEditing`
---
